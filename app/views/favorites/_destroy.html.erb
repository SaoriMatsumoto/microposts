<%= link_to favorite_path(current_user.favorites.find_by(micropost_id: micropost.id)), remote: true, method: :delete do %>
  <span class="glyphicon glyphicon-star" aria-hidden="true"></span>
<% end %>